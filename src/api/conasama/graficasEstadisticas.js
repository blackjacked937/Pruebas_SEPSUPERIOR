// APIS GRAFICAS Y PREGUNTAS CONASAMA


/*import { BASE_API_F1_COPY } from "../utils/constants";

export async function getConteoPorNivelRiesgoCategoriaApi(token) {
    try {
        const url = `${BASE_API_F1_COPY}/dashboard/Dashbord_Admin/ConteoPorNivelRiesgoCategoria`;
        const params = {
            headers: {
                'ngrok-skip-browser-warning': 'true'
            },
        };

        const response = await fetch(url, params);
        const result = await response.json();
        return result;
    } catch (error) {
        throw error;
    }
}

export async function getOpcionesPreguntaUsuariosApi(token, preguntaId) {
    try {
        const url = `${BASE_API_F1_COPY}/dashboard/Dashbord_Admin/getRangoPregunta/${preguntaId}/`;
        const params = {
            headers: {
                'ngrok-skip-browser-warning': 'true'
            },
        };

        const response = await fetch(url, params);
        const result = await response.json();
        return result;
    } catch (error) {
        throw error;
    }
}*/